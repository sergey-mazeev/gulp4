export default {
    paths: {
        built: 'built/',
        templates: 'src/templates/',
        blocks: 'src/blocks/'
    },

    browsers: ['ie >= 10', 'ff >= 29', 'Opera >= 12', 'iOS >= 6', 'Chrome >= 28', 'Android >= 2'],
}